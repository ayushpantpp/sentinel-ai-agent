# Service ownership directory

The Checkout team owns payment-api and order-api. The Identity team owns
authentication-api. The Platform Data team owns Redis and shared PostgreSQL
clusters. During an incident, page the owning team's primary on-call first and
escalate to the incident commander after ten minutes without acknowledgement.
